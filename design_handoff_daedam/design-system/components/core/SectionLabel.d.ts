/** Small tracked grey heading: 조사 진행 상황, 음성 지표, 답변별 피드백. */
export interface SectionLabelProps {
  children?: React.ReactNode;
  style?: React.CSSProperties;
}
export function SectionLabel(props: SectionLabelProps): JSX.Element;
