/** Field label 13/600. `optional` renders the grey hint (e.g. "선택 · 링크 또는 내용을 붙여넣기"). */
export interface LabelProps {
  children?: React.ReactNode;
  optional?: string;
  htmlFor?: string;
}
export function Label(props: LabelProps): JSX.Element;
