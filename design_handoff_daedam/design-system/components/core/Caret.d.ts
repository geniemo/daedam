/** Text-glyph accordion indicator. No icon font in this system. */
export interface CaretProps {
  open: boolean;
  size?: number;
  style?: React.CSSProperties;
}
export function Caret(props: CaretProps): JSX.Element;
