Secondary button — 리포트 검토, 취소, 로그아웃, 충전. On the dark interview stage use `dark` for 종료하고 리포트 받기.

```jsx
<OutlineButton size="sm" onClick={review}>리포트 검토</OutlineButton>
<OutlineButton dark onClick={end}>종료하고 리포트 받기</OutlineButton>
```
