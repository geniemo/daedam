App header for every light screen. Hidden on /interview, /analyzing, /regen.

```jsx
<Chrome name="김서연" credits={7} onHome={goHome} onAccount={goAccount} onCredits={goCredits} />
```
Only one nav item (내 면접). The credit pill is the only rounded-full element on light surfaces.
