# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/

# Node
node_modules/
dist/
.shots/

# 환경·비밀
.env
.env.local
.env.*.local
# .env를 손대기 전 떠 두는 사본. 값이 그대로 들어 있으므로 반드시 제외한다.
.env.bak*
.env.backup*
*.key

# ADK 런타임 세션·아티팩트 저장소
.adk/

# 면접 준비 데이터 (리서치 결과·질문 풀 — 사용자 데이터)
server/data/

# 녹음·산출물 (§서버 연동 6 — 답변별 녹음은 커밋하지 않는다)
recordings/
*.wav
*.opus
*.webm

# OS
.DS_Store
Thumbs.db
*:Zone.Identifier

# 서버 실행 로그 (터미널 대신 파일로 띄울 때 생긴다)
*.log

# MediaPipe 런타임·모델 (26MB). 의존성이지 소스가 아니라 커밋하지 않는다 —

# 발표·시연 영상 원본. 저장소가 아니라 드라이브에 둘 크기다 (147MB)
/video/
